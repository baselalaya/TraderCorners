// In this project we use Vite + React, not Next.js.
// Keep helper to align with requested API; always returns false.
export function isAppRouter(): boolean {
  return false;
}

export default isAppRouter;

